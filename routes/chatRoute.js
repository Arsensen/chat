const {Router} = require('express')

const router = Router()

router.post('/login', async()=>{
    try {
        
    } catch (error) {
        
    }
})

router.post('/register', async()=>{
    try {
        
    } catch (error) {
        
    }
})

module.exports = router